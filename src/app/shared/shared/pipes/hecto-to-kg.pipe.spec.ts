import { HectoToKgPipe } from './hecto-to-kg.pipe';

describe('HectoToKgPipe', () => {
  it('create an instance', () => {
    const pipe = new HectoToKgPipe();
    expect(pipe).toBeTruthy();
  });
});
